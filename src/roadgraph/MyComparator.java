package roadgraph;

import geography.GeographicPoint;

import java.util.Comparator;

public class MyComparator implements Comparator<GeographicPoint> {

    @Override
    public int compare(GeographicPoint o1, GeographicPoint o2) {
        return 0;
    }
}
